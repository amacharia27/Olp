# This variable is optional as the vite proxy handles redirection in development.
# It can be useful for production builds or if you need to reference the full URL.
VITE_API_BASE_URL=http://localhost:5000